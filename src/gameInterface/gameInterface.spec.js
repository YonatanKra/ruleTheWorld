describe("Game Integration Tests", () => {
    describe("Combat", () => {
        it('should send combatants to combat and receive a promise that results with combat results', () => {
            
        });
    });
});